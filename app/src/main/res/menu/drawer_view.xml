<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single"
        >
        <item android:id="@+id/albums"
            android:icon="@drawable/ic_album_music"
            android:title="@string/albums_string" />
        <item android:id="@+id/artists"
            android:icon="@drawable/ic_artist"
            android:title="@string/artists_string" />
        <item
            android:id="@+id/now_playing"
            android:icon="@drawable/ic_now_playing"
            android:title="@string/now_playing_string" />
    </group>

</menu>